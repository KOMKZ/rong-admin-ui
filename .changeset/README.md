# Changesets

This folder is managed by [@changesets/cli](https://github.com/changesets/changesets).

When making changes to the public API:
1. Run `npx changeset` to create a changeset
2. Describe the change (patch/minor/major)
3. Commit the `.md` file alongside your code changes
