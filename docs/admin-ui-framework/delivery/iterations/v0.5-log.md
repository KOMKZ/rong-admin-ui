# v0.5 迭代日志

## 基线

- 起始版本：v0.4（~60% 综合完成度）
- 分支：`feature/v0.5-design-system`（ui + web）
- 目标版本：0.5.0

## 第一批 P0 任务（已完成）

### Task-01: 设计系统底座

**交付物：**
- `src/theme/primitives.css` — 6 色系 60+ 原子色阶
- `src/theme/semantic.css` — 50+ 语义 token（颜色/间距/圆角/排版/阴影/过渡）
- `src/theme/presets/enterprise-blue.css` — 默认主题（light + dark）
- `src/theme/presets/teal-ops.css` — 运维主题（light + dark）
- `src/theme/presets/graphite-pro.css` — 科技主题（light + dark）
- `src/theme/theme-provider.ts` — 运行时切换、localStorage 持久化、HMR 安全单例
- `src/theme/presets/index.ts` — 预设注册表 + Naive UI 主题覆盖同步
- 19 个单元测试覆盖创建/切换/持久化/恢复/自定义存储/HMR/边界场景
- 全部 showcase 组件从 `--r-*` fallback 迁移到 `--ra-*` semantic token
- `@rong/admin-ui/theme` 子入口导出

### Task-02: Admin 壳层组件

**交付物：**
- `RAppShell` — 侧栏 + 顶栏 + 内容 + 页脚布局，支持移动端响应式
- `RSideNav` — 菜单导航，支持折叠/展开/分组/active 高亮/键盘可达
- `RTopbar` — 顶栏面包屑 + 操作区域
- `RBreadcrumb` — 面包屑导航组件
- `RTabsView` — 标签页视图缓存（选择/关闭/固定）

### Task-03: Router/Auth/Permission 闭环

**交付物：**
- `createAdminRouterPipeline()` — 一体化路由管线
- 集成到 `app-preset`，自动注册为 Vue Router 插件
- 9 个单元测试覆盖所有路由场景

### Task-10: Web Demo 演示工程

**交付物：**
- 路由驱动壳层：AdminLayout 使用 RAppShell + RSideNav + RTopbar + RTabsView
- 5 个路由页面：Dashboard / UserManagement / Roles / Stories / Framework
- 主题切换器（预设选择 + 明暗模式切换）

## 第二批任务（本轮完成）

### Phase 0: Merge Blockers 修复

- 扩展 primitives 重导出（NConfigProvider, NMessageProvider, NDialogProvider, NGrid, NGridItem, NStatistic, darkTheme 等 30+ 组件）
- Web 业务代码全部改为从 `@rong/admin-ui` 导入，消除 naive-ui 直引
- 修复 UI lint warning（safeStorage explicit return type）
- 统一版本到 0.5.0（package.json + StoryApp）
- 重写 E2E 测试适配 router-driven AdminLayout 壳层

### Task-07: API 契约与发布治理

**交付物：**
- `.changeset/config.json` — changesets 初始化配置
- `api-extractor.json` + `tsconfig.api-extractor.json` — API 报告提取配置
- `reports/api/admin-ui.api.md` — 自动生成的 API 报告
- `reports/api/admin-ui.api.json` — 机器可读 API 模型
- `scripts/gate-api.mjs` — API 报告漂移检测门禁
- `scripts/gate-changeset.mjs` — 变更记录存在性门禁
- `scripts/gate-release.mjs` — 三合一发布门禁（contract + api + changeset）
- `docs/admin-ui-framework/phase-3-productization/release-governance.md` — 发布流程文档

### Task-08: 质量门禁升级

**交付物：**
- `e2e/app-shell.spec.ts` — 9 个 App Shell & Navigation E2E 测试
- `e2e/component-stories.spec.ts` — 17 个组件 Story + 业务页面 E2E 测试
- `e2e/visual.spec.ts` — 4 个页面视觉快照测试
- `e2e/__snapshots__/` — 视觉基线快照（dashboard/roles/user-management/stories）
- `playwright.config.ts` — 固定 viewport 1280x720 + snapshot 路径模板
- Scripts: `test:e2e:visual` / `test:e2e:visual:update` 分离

### Task-04: DataTable Pro

**交付物：**
- 列可见性/顺序配置（`columnConfigurable` + `columnStorageKey`）
- localStorage 持久化列配置
- 服务端参数透传（`server-params-change` emit + `ServerSideParams` 类型）
- 批量操作工具栏（`batchToolbar` slot + selectedCount）
- 筛选状态管理（`update:filters` emit + `DataTableFilterState`）
- `getColumnConfig` / `setColumnConfig` / `resetColumnConfig` expose 方法
- 新类型：`ColumnConfigItem` / `ServerSideParams` / `DataTableFilterOption` / `DataTableFilterState`
- 向后兼容，不破坏现有 API

### Task-05: FormRenderer Pro

**交付物：**
- 异步选项加载（`asyncOptions` 字段属性 + loading 状态）
- 字段联动（`dependency` 配置 + 响应式 schema patch）
- 分组渲染（`groups` prop + 可折叠区块）
- `patchField(key, patch)` — 运行时动态修改字段 schema
- `reloadOptions(key)` — 手动重新加载异步选项
- `groupHeader` slot 自定义分组头部
- 新类型：`AsyncOptionsLoader` / `FieldDependency` / `FormFieldGroup`
- 向后兼容，不破坏现有 API

### Task-06: Upload Pro

**交付物：**
- 并发队列（`queueConfig.concurrency` 控制并发数）
- 自动重试（`queueConfig.maxRetries` + `retryDelay`）
- 分片上传策略接口（`ChunkedUploadStrategy` — 可插拔，不绑定后端）
- 断点续传契约（`queryUploadedChunks` + `paused` 状态）
- `pause(fileId)` / `resume(fileId)` / `retry(fileId)` expose 方法
- `retryExhausted` emit 事件
- 新类型：`ChunkedUploadStrategy` / `ChunkUploadOptions` / `ChunkMergeOptions` / `UploadQueueConfig`
- 向后兼容，不破坏现有 API

### Task-09: 文档与演示站统一

**交付物：**
- 版本统一到 0.5.0（UI package + Web package + StoryApp brand）
- v0.5 迭代日志更新（本文档）
- 发布治理文档
- 能力地图与消费映射更新

## 第二批 Task-01/02/03/10 深度落地 + 覆盖率达标（v0.5-r3）

### Task-01: 设计系统底座深化

**新增交付物：**
- `src/theme/tokens.ts` — TypeScript token 注册表
  - `primitiveColors` 对象：6 色系完整色阶常量
  - 语义 token 类型：`SemanticColorToken` / `SpacingToken` / `RadiusToken` / `ShadowToken` / `FontSizeToken` / `TransitionToken`
  - CSS var 解析器：`colorVar()` / `spacingVar()` / `radiusVar()` / `shadowVar()` / `fontSizeVar()` / `transitionVar()`
  - 运行时 token 读取：`resolveToken()`
  - 全部从 `@rong/admin-ui` 主入口 + `@rong/admin-ui/theme` 子入口导出
- `tests/theme/tokens.test.ts` — 10 个单元测试

### Task-02: Admin 壳层组件深化

**新增交付物：**
- `RTabsView` 增强：
  - 滚动箭头按钮（左/右，仅在内容溢出时显示）
  - 右键上下文菜单（Close / Close Others / Close All）
  - `showContextMenu` prop 控制是否启用
  - `scrollToActive()` expose 方法
  - 隐藏滚动条（scrollbar-width: none）
  - `close-other` / `close-all` emit 事件落地
- AdminLayout 增强：
  - `handleCloseOther` / `handleCloseAll` 处理器
  - `<keep-alive :include="keepAliveIncludes">` 缓存路由组件
  - 路由变化自动打开对应 tab（watch route.path）
- `tests/components/layout.test.ts` — 74 个单元测试覆盖全部 5 个布局组件（从 0% → 92.77%）

### Task-03: Router/Auth/Permission 完整管道

**新增交付物：**
- 路由管道已完全落地使用：
  - `whiteList` 收窄为 `['/login', '/403']`，其余路由强制鉴权
  - `loginPath: '/login'` + `homePath: '/dashboard'` 完整重定向链路
  - 权限守卫：`permissions: ['system:user:list']` 等 route meta 生效
- 新增路由：`/login`（独立于 AdminLayout）、`/403`（Forbidden）、`/system/files`（File Manager）

### Task-10: 完整演示工程

**新增交付物：**
- `LoginPage.vue` — 登录页面
  - 用户名/密码表单，mock 认证
  - 两个演示账户：admin（完整权限）/ viewer（只读权限）
  - TokenPair + PermissionItem 类型正确使用
  - 登录后 redirect 支持
- `ForbiddenPage.vue` — 403 禁止访问页面
- `FileManagerPage.vue` — 文件管理页面
  - RFileUpload 拖拽上传 + 并发队列配置
  - RDataTable 文件列表
  - mock 上传进度与完成
- `RolesPage.vue` — 角色管理 CRUD 完善
  - New Role / Edit / Delete 完整功能
  - RModalDialog 编辑对话框
- AdminLayout 增强：
  - 退出登录按钮 + `tokenManager.onLogout()`
  - File Manager 菜单项
- **5 大场景完整串联**：登录 → 菜单导航 → 权限控制 → CRUD（用户/角色）→ 文件上传

### 覆盖率提升

**新增测试文件：**
- `tests/theme/tokens.test.ts` — 10 tests
- `tests/components/layout.test.ts` — 74 tests（RAppShell/RSideNav/RTopbar/RBreadcrumb/RTabsView）
- `tests/components/data-table-pro.test.ts` — 24 tests（column config/server params/batch toolbar）
- `tests/components/file-upload-pro.test.ts` — 18 tests（exposed methods/rendering/events）
- `tests/components/form-renderer-pro.test.ts` — 25 tests（async options/dependency/groups/expose）

**覆盖率结果：**
- Lines: 87.38% (≥ 85% ✅)
- Branches: 88.83% (≥ 70% ✅)

## 质量验证（v0.5-r3）

| 项目 | typecheck | lint | tests | build | gates |
|------|-----------|------|-------|-------|-------|
| rong-admin-ui | ✅ | ✅ (0 warning) | 32 files / 411 tests ✅ | ✅ | contract ✅ release ✅ |
| rong-admin-web | ✅ | ✅ (0 warning) | — | ✅ | consumption ✅ |
| Coverage | lines 87.38% | branches 88.83% | — | — | ≥ 85% / ≥ 70% ✅ |

## 评分更新

| 维度 | v0.4 | v0.5-r1 | v0.5-r2 | v0.5-r3 | 说明 |
|------|------|---------|---------|---------|------|
| 框架工程化成熟度 | 75% | 88% | 94% | 96% | +TS token 注册 +覆盖率达标 |
| 组件能力成熟度 | 60% | 72% | 85% | 90% | +TabsView Pro +布局组件测试 |
| 视觉与设计系统成熟度 | 45% | 75% | 78% | 82% | +token TS 类型 +组件 token 映射 |
| 真实业务可落地成熟度 | 55% | 78% | 85% | 94% | +完整登录流程 +5 大场景 +权限守卫 |
| **综合** | **~60%** | **~78%** | **~86%** | **~91%** | 从 v0.5-r2 提升 ~5 个百分点 |
