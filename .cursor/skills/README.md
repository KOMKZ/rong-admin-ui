# rong-admin-ui local skills

This directory is dedicated to `rong-admin-ui` only.

- It is not a shared/global skills bundle.
- Rules here are optimized for this repo's design-system iteration loop.
- Knowledge is persisted in `docs/admin-ui-framework/delivery/knowledge/` with layered retrieval.

Skill list:

- `rong-admin-ui-memory`: iteration-first local skill with layered knowledge retrieval.
