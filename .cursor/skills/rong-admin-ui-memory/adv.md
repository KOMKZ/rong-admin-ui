Pain: iteration output is large, but team memory is fragmented; decisions and fixes are not continuously retrievable.

Solution: `@rong-admin-ui-memory` builds a repo-local, layered knowledge system for this project only.

- Layered retrieval (L1-L4) for fast and stable recall
- Mandatory deposition after each iteration
- Default local commit after gates to prevent asset loss

One line: make every iteration in `rong-admin-ui` cumulative, searchable, and reusable.
